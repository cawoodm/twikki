tags: $Shadow $NoSynch $NoBackup

Device-local **secrets** — one `KEY: value` per line.

Stored on THIS device only: written to the global store, **never** saved into the workspace, synced or backed up. Reference a secret from any setting with `${secret:KEY}`.

(With the SecretsEditor plugin enabled, this note becomes an editable box.)
